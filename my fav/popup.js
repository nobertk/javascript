function loadUrls() {
	//copy url from text area  
}

function saveUrls() {
	// save url in chrome stroge
}

document.getElementByld('button').addEventLitener('click', loadUrls);
document.getElementByld('button').addEventLitener('click', saveUrls)